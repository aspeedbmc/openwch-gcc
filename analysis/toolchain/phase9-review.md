# phase-9 终局独立对抗审计报告

审计者上下文从未持有 phase-9 的执笔工作。p8/P8-R 两轮审计的基线知识（封存值、判据器缺陷族、DEC 台账）在此作为审计资产使用。一切声称按「可复算即自己复算」处理：本报告每条结论要么来自我自己跑的命令，要么标注为「未能核验」。任务书、里程碑简报、round-report、regression.md、DECISIONS 追录条一律作为**被审对象**，不作可信来源。

审计日期 2026-08-18。工作目录 `/Users/apple/Projects/openwch`。

---

## 0. 结论摘要

| 项 | 值 |
| --- | --- |
| **verdict** | **有条件放行** —— 使命面（rv64 与各自官方对齐、rv32 零扰动）经我独立复算**全部成立**；缺陷全部落在**证据件、判据纪律与叙述**层，无一影响补丁语义或 gate 数字 |
| P0 | **0** |
| P1 | **2** |
| P2 | **9** |
| P3 | **13** |
| 独立锚定 | clean-room 复放 **6/6**、patch-id **32/32**、封存 **20/20 ×2**、我方侧 quick **274/274 ×2**、rv64 官方对拍 **6/6 arch × 两版本**、rv64 半字判据 **5/5 ×2（我复跑）** |

**一句话**：本轮的**技术结论是对的，而且比它自己给出的证据更站得住**——我用两条独立路径复算了它最关键的两个结论（rv64 对齐、rv64 半字判别力），都成立；被扣分的是「三条独立证据」里有两条对被归因的机制没有判别力，以及一份判据器产物过期后未重生成。

---

## 1. 覆盖矩阵：我亲自复算的项

| # | 项 | 方法 | 结果 |
| --- | --- | --- | --- |
| 1 | 两版本 pristine 复放 | p8 审计同一份 clean-room 脚本（`git archive` 根 → `git init` → `git apply --index` → `write-tree`，不碰镜像） | **6/6 命中**：15.2.0 binutils **`1321f9e24fd6843db33411451b0d382260f20cb0`**、12.2.0 binutils **`0d01a497ae860ce540c463320ce0a4436e880a05`**；两版 gcc 树 `5bb6a456…`/`af74531c…` **逐位未动**；8.2.0 两树未动 |
| 2 | patch-id | 32 片重算对 `patch-id.tsv` | **32/32** 相同 |
| 3 | patch-id 变化计数 | 与 HEAD 基线逐片对比 | 15.2.0 **14 不变 / 2 变**（0002、0007）；12.2.0 **15 不变 / 1 变**（0004）——与声称逐一相符 |
| 4 | 交付面封存 ×2 | `shasum -c` | 15.2.0 **20/20 OK**（自 `80f49e18…`）、12.2.0 **20/20 OK**（自 `b2b4afd0…`）；两件头部均自述 seal-time 双树且与镜像现值一致 |
| 5 | 封存后 mtime 漂移 | `stat` 全量 `patches/**` vs 封存时点 | **零漂移**：15.2.0 seal 04:07:59 > 最新件 00:11:08；12.2.0 seal 00:32:16 > 最新件 00:17:44 |
| 6 | 守卫进出场 | 相对路径口径重算 + `readlink` | 15.2.0 `30f8ba45…`、12.2.0 `dccdf8a5…` **逐字等于 `guard-s2exit.tsv`**；8.2.0 `89876c5c…` **逐字等于进场基线**（未触碰成立）；`toolchain-current`/`mrs` 均为基线值 |
| 7 | **rv64 使命面** | 官方 vs 我方 install 树，6 组 arch × rc/stderr/目标文件/三模式反汇编，两版本 | **两版本各 6/6 全同**（详见 §2.1） |
| 8 | rv64 判别力自证 | 同一探针打 **清理前树**（p8r） | p8r 在 rv64 下仍 `rc=1 unrecognized opcode … extension` ⇒ **探针有判别力，通过非空绿** |
| 9 | **rv32 零扰动** | `evt-compare.sh` 打**我方 phase-9 树**（每版本一次） | 15.2.0 **274/274/0**、12.2.0 **274/274/0**，`aux_diff=4` 全 `.map`（DEC:17 既裁豁免）；symlink 前/中/后均基线值 |
| 10 | rv64 半字判据 J1–J5 | 用**现行判据器**重跑两版本 `summary.json` | **两版本各 5/5 PASS**；12.2.0 增量 `{OURS:8704, WCH:8704}`（见 §3 P2-1） |
| 11 | rv64 半字数据独立重数 | 自写脚本对 24 个 `decoded.tsv.gz` 数 XW 助记符 | `xw` 模式 8960/cell vs `default` 256/cell，增量 **+8704**，**OURS==WCH 全 24 cell 相同** |
| 12 | **P9-F1 时序** | `git log` + `merge-base --is-ancestor` + `git show 60ebcda:<file>` | d681881 `2026-08-17T21:31:45+09:00` **是** 60ebcda `2026-08-17T23:12:16+09:00` 的祖先；60ebcda 当时该处仍为 `NF != 3` 三列断言 ⇒ **缺陷属实**；p8 linux 腿跑于 16:51–17:51，早于两者 ⇒ **p8 结论无损成立** |
| 13 | P9-F1 修法 | 读两处现文 | `build-toolchain-15.2.0-linux.sh:113` 与 `tmp/phase3h/gate-prepare_linux.sh:101` 均已改为**四列 + 校验第四列名 `source_commit`** |
| 14 | 4c 删除面 | `git diff` 脚本 + 两侧 `gcc-all-gcc.log` 实读 | 删除内容为 `make all-gcc TFLAGS='-march=rv32imac_zaamo_zalrsc -mabi=ilp32'` 等 4 处；**该 flag 是活的**（见 §3 P2-2） |
| 15 | ra-flags 常设护栏 | 自跑 p8 审计的 reproducer 打 p9final | `OFF == P9` **逐字节相同**；`OFF != P8`（旧缺陷树仍被判别）⇒ **护栏保持判别力**，P8-R 的回归修复在本轮未被破坏 |
| 16 | gas 账目 | 读 `gas.sum` | 15.2.0 **653** expected passes / 23 xfail / 9 unsupported（= 650+3）；12.2.0 **213**（= 210+3） |
| 17 | 新增 rv64 用例 | 列镜像 testsuite 目录 | 15.2.0 `xw-compressed-rv64.d` / `xw-compressed-rv64-fail.d(.l)` / `xw-compressed-dis-rv64.d`；12.2.0 `wch-xw-compressed-rv64.d` / `wch-xw-disassemble-rv64.d` / `wch-xw-rv64-fail.d(.l)` ⇒ **各 3 条、共 6 条**，且**命名分版本**（符合 M1「期望文本各取各官方禁统一」） |
| 18 | From 行统一 + 四列台账 | 读三版本首行与表头 | 三版本 `patch-id.tsv` 均为 `component/patch/stable_patch_id/source_commit` 四列；12.2.0 `From` 已由全零改为真实 sha `0e3a61c5810f…` ⇒ closure §7② 落地，**我 p8 报告的 P3-17 至此闭合** |
| 19 | 上界声称 | 对现文全量 `git diff` 分类 | 除两个 XW 表片外，其余 14+15 片的改动**全部只有 `From`/`index` 头行**（不入 patch-id）；见 §2.2 |

---

## 2. 独立锚定结果

### 2.1 rv64 使命面：两版本各 6/6 与官方全同

对 6 组 `-march/-mabi` 配置（`rv32imac_xw`/`rv64imac_xw`/`rv32imac`/`rv64imac`/`rv32imafdc_xw`/`rv64imafdc_xw`），比较**官方 vs 我方**的 `rc`、`stderr`、目标文件字节、以及 `default`/`-M xw`/`-M xw,no-aliases` 三模式反汇编：

| 版本 | 结果 |
| --- | --- |
| 15.2.0 | **PASS=6 FAIL=0** |
| 12.2.0 | **PASS=6 FAIL=0** |

rv64 关键面逐字复现（`c.lbu/c.lhu/c.sb/c.sh`，`-march=rv64imac_xw -mabi=lp64`）：

| 侧 | rc | `-M xw` 反汇编 |
| --- | --- | --- |
| 官方 15.2.0 | 0 | `2188 lbu a0,0(a1)` / `218a lhu` / `a188 sb` / `a18a sh` |
| **我方 15.2.0（p9final）** | 0 | **逐条相同** |
| 我方 15.2.0（p8r，清理前） | **1** | `unrecognized opcode … extension` ⇒ 探针判别力自证 |
| 官方 12.2.0 | 0 | `2188 lbu a0,0(a1)` / `218a lhu` / `a188 sb` / `a18a sh` |
| **我方 12.2.0** | 0 | **逐条相同** |

⇒ **使命达成，且 12.2.0 官方 rv64 面与 15.2.0 官方同形是实测事实**（不是假设）。M1 的「两版本 xlen 统一 0」在**结论层**成立；其**推导是否独立**见 §4 的分工结论。

### 2.2 S2 改动面：上界声称对现文成立

| 版本 | 表项片 | 其余片 |
| --- | --- | --- |
| 15.2.0 | `binutils/0002` +34/−19、`binutils/0007` +22/−11 | 其余 14 片仅 `From`（0003 另有 `index` 行，因前片行数位移）——**不入 `git patch-id --stable`** |
| 12.2.0 | `binutils/0004` +32/−19 | 其余 15 片仅 `From` 行 |

对现文核实「**march / 属性 / 门 / 开关不触碰**」：XW 表项 diff 中 32→0 的改动逐行只落在 **xlen 列**（`{"lbu", 32,` → `{"lbu",  0,`，八形式 × 别名/非别名 = 16 行/版本），未出现 `riscv_supported_*`、`arch_attr`、`.attribute`、`md_parse_option`、`OPTION_*`、`riscv_set_arch`、`xw_disassemble`、`pd->xw`、`riscv_multi_subset_supports` 的任何增删。**上界声称成立。**

**patch-id 局限性（协调器点名项）**：14/16 与 15/16 不变**只**证明「未触碰的片其 diff 字节未变」；对**变了的**那 2 片 / 1 片，patch-id 变化是零信息量的——它变当且仅当 diff 变，不指示变了什么。本轮的改动面结论因此**不能**由 patch-id 承担，实际由三件独立证据承担且我已各自复算：①终态 tree（clean-room 复放命中）②逐行 diff 分类（本节）③行为面官方对拍（§2.1）。**证据结构本身是健全的**，此处仅记该指标的解释边界。

---

## 3. 发现列表（按严重度；不过滤）

### P1-1 `[P1][置信度 高]` 4c 判据在首次测量 FAIL 之后被整体替换，替换物对其命题**零判别力**，且未按预注册**强制**的方式登记；报告称其为「协调器批准的修订版」，而协调器批准的恰是被弃用的那一条

三层，每层我都实开核对：

**① 预注册明文禁止这种替换，且点名 p8 教训。** `s2-preregistration.md:4`：

> 本文件在 S2 与 S3 之间不得改写；如需修订，只能追加带日期的修订段并说明理由（p8 SR-02「测量后换判据列」教训）。

E 节原判据 `:85-86`：`T2 = 构建日志中 gcc 编译命令行 diff 为空`；`T3 = T2 的 diff 件落证据树并在轮报告单列（4c 与 xlen 同批重建，**命令行 diff 是把两者归因分开的唯一凭据**）`。
2026-08-18 追加段 `:98-122` 只有 **G/H/I** 三节，**无一涉及 4c**。

**② 实际交付的是完全不同的 T2a/T2b/T2c，且替换发生在测量失败之后。** `tflags-4c.judge.v1-FAIL.txt`（v1 用原判据「命令行集合」）报 FAIL（before 3504 / after 2056 / 差 1584 行），随后判据改为 T2a/T2b/T2c。**「唯一凭据」被弃用。**

**③ 替换物对其命题零判别力（我实测）。**

| 腿 | 我的核验 |
| --- | --- |
| **T2a** | `tflags-attribution.sh:39` 在 `gcc-all-gcc.log` 数 `TFLAGS=`。**before 侧（p8final/p8r，驱动确曾传 `TFLAGS="$gcc_build_tflags"`）该日志独立命中也是 0**——make 不把命令行变量回显进阶段日志。**取何值都返回 0** |
| **T2b** | `:29-32` 只比 `-o *.o` **文件名**集合，脚本注释自陈为免疫并发交错而降格；同一批名字在两种 flag 下都会产生 |
| **T2c** | 论「`TFLAGS` 不进宿主编译标志」——被删注释从未如此声称，它称的是**目标侧** xgcc multilib 探针 |

**④ 现成的判别信号存在，且与「无影响」叙述相反**：同两份日志 `grep -c zaamo_zalrsc` = **before 12 / after 5**，差掉的 7 条正是 `…/xgcc -B…/ -march=rv32imac_zaamo_zalrsc -mabi=ilp32 …` 探针调用（`:97544/97549/97550/97553/97732/97843/97844`）。**被删代码是活的**（`git diff` 见 `make all-gcc TFLAGS="$gcc_build_tflags"` 等 4 处）。

**⑤ 措辞把两次不同的修订混为一谈。** `regression.md:16` 与 `checklist:89-90` 称「判据按**协调器批准的修订版**兑现」，而 `DECISIONS.md` 2026-08-18 M2 记录的批准口径是「rv32 gate 传递性等值 + **构建命令行 diff 归因把手**」——**恰恰包含被弃用的那一条**。

**失败场景**：验收方按 DECISIONS 口径复核 4c，找不到被批准的「构建命令行 diff」证据；现存 PASS 由两条恒真判据推出，支撑不了「删除无影响」。
**结论仍站得住，但靠的是别的**：构建在无该脚手架下成功 + rv32 gate 产物对封存值逐字节相同（我已独立锚定 274/274 ×2）⇒ 传递性产物等价。
**修法**：把 T2 替换作为独立偏差登记（性质 = 测量后换判据 + 判据无判别力），补写预注册的日期修订段并向协调器补批；把 4c 结论明确改挂到传递性等值；如实登记 `zaamo_zalrsc` 12→5 为「构建期可见、产物不可见」。若要保留一条有判别力的直证，最小做法是对同一棵树带/不带 TFLAGS 各跑一次 `all-gcc` 并比对宿主 `.o` **内容哈希**。

### P1-2 `[P1][置信度 高]` 失败留痕纪律在 DEV-P9-07 立规之后再次被违反（原文永久丢失），且轮报告把停机数报少了

**留痕丢失**：`deviations.tsv:11`（DEV-P9-09）、`:12`（P9-F1）、`:13`（DEV-P9-10）三条的证据指针分别指向 `s3/prepare.stderr` 与 `s3/linux-build.stderr`——我实测两者**均为 0 字节**（被最终成功跑覆盖）。linux run1（NS 陈旧根）与 run2（台账列数）的报错原文磁盘上**不存在**。而 DEV-P9-07 立规（12.2.0 halfword 时段）早于 linux run1，属**立规后再犯且未登记**。

**停机低报**：盘面 `linux-results.run{1..6}-FAIL-*.tsv` **六份**俱在（prepare / ledger / staleroot / regen / build / xwlto），加 darwin 侧两次（`darwin-results.run1-FAIL-at-sr02.tsv`、`resume2-FAIL-order.tsv`）**共 8 次**；而 `15.2.0/round-report.md:93` 写「**三次**停机全部 fail-closed」。其中 run3（`fresh root already exists…`）在 12 条台账里的归属存疑。

**失败场景**：协调器按台账指针复核最关键的两次 linux 停机，只能打开两个空文件；按轮报告理解本轮稳定性，会低估近三倍。
**修法**：三条指针改指实际存活件（`linux-results.run*-FAIL-*.tsv`、`prepare.run{3,4}-FAIL.stderr`、`linux-build.run5-FAIL.stderr`），注明 run1/run2 原文不可复算；把「三次」改为实数并附对照表；run3 若无归属则补登条目。

### P2-1 `[P2][置信度 高]` 12.2.0 的 rv64 半字判据产物停在**修复前**的判据器版本，盘面写 `J5 FAIL / 4-5 PASS`，而简报报 `5/5`

**位置**：`tmp/phase9-evidence/12.2.0/s3/halfword-rv64.judge.txt`（mtime 08-18 00:26）vs `tmp/phase9-evidence/briefs/S3-milestone.md:59`

盘面产物原文：

```
[FAIL] J5 判别力：-M xw 模式中 0 个 cell 真的解出 XW 助记符
words_total=1572864  合计 4/5 PASS
```

而 S3 简报 §4 写「12.2.0：J1–J5 **5/5**」，§4 末又写「判别力（must-fire 8/8 + J5 增量自证，**全部 fail-closed**）」。目录下**没有**修正后的重跑（`find` 全量确认，`halfword-rv64.v1-repro` 是 DEV-P9-07 的根因复现件，非重跑）。

**我把实质查清了，结论是简报的数字对、产物错**——两条独立路径：
1. **自写脚本重数** 24 个 `decoded.tsv.gz`：`xw` 模式 8960 命中/cell、`default` 256/cell，增量 **+8704**，且 **OURS 与 WCH 在全部 24 个 cell 上逐一相同**。
2. **用现行（已修）判据器重跑**同一份未改动的 `summary.json`：**5/5 PASS**，`xw 增量 {'OURS': 8704, 'WCH': 8704}`，与 15.2.0 同值。

**根因**：现行 `probes/halfword-judge.py` 的注释自陈「首版按整行 split 把 raw 拆碎，计数恒 0——本判据器自身的假阴，**已修**」。15.2.0 的产物由修后判据器生成（5/5），**12.2.0 的产物未随判据器修复重新生成**。两版本 `decoded.tsv` 行格式我实测逐字同构，可排除版本格式差异。

**为什么仍要记 P2 而非 P3**：①盘面唯一的判别力证据对 12.2.0 是 FAIL，协调器「现场复测」若抽到该件会得到与简报相反的结论；②该判据正是本阶段用来防「空对空假绿」的那一关（DEV-P9-04 的修法），其产物失效等于该防线在 12.2.0 侧盘面无证；③判据器修复后**只重生成了一个版本**，属不对称补救。**若实质是错的，这将是 P0/P1——盘面无法区分二者，只能靠重算。**
**修法**：用现行判据器对 12.2.0 重跑并覆盖 `halfword-rv64.judge.txt`（数据无需重跑，已证充分）；简报同步注明产物版本。

### P2-2 `[P2][置信度 高]` 4c 的「三条独立证据」没有一条能判别被归因的机制，而一个现成的判别信号与「无影响」叙述相反

**位置**：`tmp/phase9-evidence/probes/tflags-attribution.sh:36-49`、`tmp/phase9-evidence/15.2.0/s3/tflags-4c.judge.txt`、`briefs/S3-milestone.md:65-73`

被删的不是死代码。`git diff scripts/build-toolchain-15.2.0.sh` 显示删除的是**活赋值**：

```
-gcc_build_tflags='-march=rv32imac_zaamo_zalrsc -mabi=ilp32'
-    make -j"$build_jobs" all-gcc TFLAGS="$gcc_build_tflags"
-    make install-gcc TFLAGS="$gcc_build_tflags"
```

三条证据逐条检验：

| 腿 | 声称 | 我的核验 |
| --- | --- | --- |
| **T2a** | 两侧「独立 `TFLAGS=` 赋值」均 0 次 | **由构造决定的平局，零判别力**。判据器读 `<log>/gcc-all-gcc.log`；而 **before 侧（p8final/p8r，确实带 TFLAGS 构建）该日志的独立 `TFLAGS=` 命中也是 0**——make 不把命令行变量回显进阶段日志。**仪器看不见它所归因的量**，与 DEV-P9-04「空对空」同型 |
| **T2b** | 宿主目标文件集合 1747==1747、`.o` 集合 2106==2106，差异 0 | 比的是**目标文件名集合**，非内容/编译标志（脚本注释自陈为免疫并发交错而「只取 `-o` 后的 `*.o` 词」）。同一批文件名在两种 flag 下都会产生，**对本问题天然不敏感** |
| **T2c** | 源码依据：`TFLAGS` 只进 `GCC_FOR_TARGET` 与 `*_TARGET_EXPORTS`/`STAGE*_TFLAGS`，**从不进宿主编译标志** | 论断为真但**答非所问**：被删注释从未声称它影响宿主 CFLAGS，它声称的是「让 all-gcc 的临时 xgcc 探针先选中 multilib」——**目标侧**机制，三条腿都没碰 |

**现成的判别信号，两侧不同**：同两份日志里 `grep -c zaamo_zalrsc` = **before 12 / after 5**。差掉的 7 条正是

```
97544: …/xgcc -B…/ -march=rv32imac_zaamo_zalrsc -mabi=ilp32 …
97549/97550/97553/97732/97843/97844: 同类 xgcc 探针调用
```

after 侧只剩 multilib 表自身的 5 条。即**删除确实改变了构建期 xgcc 探针的调用**——恰是被删注释描述的那个机制。简报 §5 的「三条独立证据 ⇒ 4c 删除为无效改动」在**产物层**成立，在**构建命令层**不成立，而三条腿都无法分辨这一点。

**结论仍然站得住，但靠的是别的东西**：①构建在无该脚手架下**成功**，经验性地退役了注释所述的必要性；②rv32 gate 产物对 P8-R 封存值**逐字节相同**（我已独立锚定 274/274 ×2）+ rv64 面对齐 ⇒ 传递性产物等价。建议把 4c 的结论明确挂到这两条上，并把 `zaamo_zalrsc` 12→5 如实登记为「构建期可见、产物不可见」的差异。

### P2-3 `[P2][置信度 高]` 宿主二进制不可复现，使「源码不变 ⇒ 行为不变」类论证在本轮仍缺一环（p8 RV-2 未消化）

gcc 源码树在 p8r 与 p9final 之间**逐位相同**（均 `5bb6a456…`），但两棵 install 树的 **`cc1`/`cc1plus`/`lto1`/`gcc`/`lto-dump` 全部字节不同**。这与 p8 审计 RV-2 记录的 binutils 侧现象同源：**本项目的宿主构建不是比特可复现的**。

后果：8.2.0 轮曾用「重建前后 `cc1` 同为 `d57e7de2…`」作为 C 级改动无害的**最强旁证**，那条路线在 15.2.0 darwin 上**不可用**；4c 因此只能退到产物传递性（P2-2）。这不是 phase-9 制造的缺陷，但它**静默地取消了一个证据选项**，两轮均未登记。
**修法**：在 closure/RR 登记「15.2.0 darwin 宿主构建非比特可复现」这一事实及其对证据选择的约束；或为该平台建立一次可复现性基线（同源同路径重建两次比对），把这条路线要么修复要么正式关闭。

### P2-4 `[P2][置信度 中]` 里程碑简报是协调器验收的唯一入口，但它与盘面产物之间没有交叉核对环节

P2-1 的存在方式说明了这个结构问题：S3 简报的 11 个 lane 数字全部由执行者转录，协调器按简报裁定（DECISIONS 2026-08-18 追录条），而**盘面产物与简报不一致时无人发现**。本轮我抽到的 11 项数字里有 10 项与产物一致、1 项（12.2.0 J5）相反——比例不高，但它恰好落在判别力这一关。
**修法**：简报的每个数字标注其产物路径（本轮多数已标），并在里程碑验收前跑一次「简报数字 vs 产物」的机械对照（可脚本化：抽取简报表格中的数字，逐一 grep 对应产物）。

### P2-5 `[P2][置信度 高]` 落盘的 `s1-probe.py` 不是产出 S1 证据的那一版，`raw-*.log` 不可复放

`tmp/phase9-evidence/s1-probe.py:325-329` 传 `os.path.basename(obj)` 且 `cwd=workdir`，注释明写此举是为避免 objdump 横幅把 run-tag 写进 raw log。但落盘证据 `15.2.0/s1/runA/raw-official.log` 的横幅实为

```
tmp/phase9-evidence/15.2.0/s1/runA/work/p-official.o:     file format elf32-littleriscv
```

——**含 run-tag 的相对路径**，传裸文件名的 objdump 不可能产出。旁证：`s1-probe.py` mtime `2026-08-18 00:18:20`，晚于四次 run（`23:43:45`–`23:44:01`）约 35 分钟。

**失败场景**：按 spec §8 指针重跑复放 S1，`probes.tsv` 会相等而 `raw-*.log` 必不等（约 1872 变更行），复放者无法区分「证据被改」与「脚本被改」。15.2.0 spec.md:47-48 自述的「raw-*.log 仍有 2080 行 run-tag 差异」与落盘脚本（应产 0 行）自相矛盾。
**血量有限**：判据列取自 `probes.tsv`，`raw-*.log` 非判据面；且 S1 的**结论**可随时用官方二进制重新导出（我与分工单元各做了一次，见 §4.1）。
**修法**：把真正产出证据的脚本版本落盘（或另存 `s1-probe.as-run.py` 并在 spec §8 指明），当前修订版标注「未用于 S1 证据」，spec §2 残差记述随之改写。
（分工单元将本条评为 P1；我下调为 P2，理由是判据面不受影响且结论可独立重导——但盘面确实无法自证复放，故不低于 P2。）

### P2-6 `[P2][置信度 高]` 12.2.0 的「我方侧」S1 证据今天已不可复算（侧参纪律两版本不对称）

`12.2.0/s1/runA/provenance.tsv:6` 记 ours `as` = `2935071f4f6decdaf…`，路径指向**活构建树** `…/build/darwin-arm64/application/bin/riscv-wch-elf-as`。我实测该文件今为 `d57ca5a0c30e4c40…` ⇒ **已被 S2 重建覆盖**。对照 15.2.0 的 ours 侧钉在冻结树 `ours-p8r-frozen`，其 `b27619c0…6aea` **今天仍精确命中**。

**后果限定（我已量化）**：不可复算的是 12.2.0 的**改前基线**——即「342 → 0」中的 **342** 与 A/B/C/D 分类的我方半边；而**改后的 0** 我已用官方二进制独立复现（§2.1 的 6/6 全同）。**规格结论不受影响**：规格只由官方侧 rv32↔rv64 比较推出，官方二进制的 provenance 我逐项复核全部命中。
**修法**：S1 侧参先冻结再探测；已发生的，在 spec §1 与 round-report 显式标注「12.2.0 我方侧随 S2 重建失效，分类计数以 runA/probes.tsv 为唯一存档，不可复算」。

### P2-7 `[P2][置信度 高]` phase-9 实际在写 p8 证据树，而 DEV-P9-09 声称「修后复查全文件无 phase8 残留常量」

`15.2.0/s3/replay.sh:15` 仍为 `SCRATCH="$REPO/tmp/phase8-evidence/15.2.0/scratch"`，且该脚本被 `run-darwin.sh:252` 与 `resume-from-sr02.sh:144` **实际执行**（`replay.sh` 内还对该路径做 `rm -f`）。实证：`tmp/phase8-evidence/15.2.0/scratch/idx-s3-{gcc,binutils}` 的 mtime 为 **Aug 18 01:06**，落在 phase-9 时段。DEV-P9-09 的处置句因此不成立（12G 树确未触碰，但 p8 证据树被写入）。同族潜伏：`probe-ra-flags.sh:23` 默认输出根仍写 `tmp/phase8-evidence/…`（本轮显式传参未触发）。
**修法**：处置句改为可核窄口径（「`run-linux.sh` 内 NS 常量已改」），把 `replay.sh:15` 作为 DEV-P9-09 同族第二实例补登。

### P2-8 `[P2][置信度 高]` `assert-sr02.py` 的账目算式仍是 p8 版——侧表族**第三处**未适配常量，而 DEV-P9-08 声称三处已全改

`15.2.0/s3/assert-sr02.py:61-63` 恒输出 `sides 6 -> 7; 6^2*8=288, 7^2*8=392`，`:131` PASS 行硬编码 `（账目 288->392 由加侧解释）`，而 `:36` `EXPECT_ROWS_NEW = 512`。落进证据的 `sr02-assert.txt:1` 于是同时出现 `new=512` 与 `7^2*8=392`。我实测最终矩阵为 **8 侧 / 512 行**（= 8²×8），简报的 `8²×8` 才对。同源：`assert-sr03.py:11` 注释仍写「加 OURS-P8 侧」。
DEV-P9-08 自陈「全仓复查其余 runner 均直接迭代 SIDES、无第二张侧表」——**漏了这处**。它本身正是 DEV-P9-08 提炼教训的第三个实例。
**修法**：`:61-63` 改为按新矩阵实际侧数推算、`:131` 改为格式化输出；作为 DEV-P9-08 第三实例补登。

### P2-9 `[P2][置信度 高]` 出场守卫缺 `dirlist` 项，`/Users/wch/Work` 不变是无证据断言

`regression.md:71-75` 标题为「出场守卫（与 `guard-s2exit.tsv` 逐项对照）」并断言 `/Users/wch/Work` 目录列表不变；但 `guard-s2exit.tsv` 全文**无 dirlist 行**（`dirlist` 只出现在进场侧 `guard-baseline.tsv:15-16`），`s3/symlinks.txt` 与 `linux-symlinks.txt` 也只记两条 readlink。这恰是 DEV-P9-10（容器内建 `/home/wch` 别名）的承重缓解论证所依赖的对象。我受审计边界限制（只读本仓库目录）无法代为补证。
**修法**：补一份含两条 dirlist 的 S3 出场守卫件，或把断言降级为「未执行任何写 `/Users/wch` 的命令」并给命令级依据。

### P3 级

| # | 发现 | 位置 |
| --- | --- | --- |
| P3-1 | 守卫内容哈希口径在 phase-8（**绝对路径**）与 phase-9（**相对路径**）之间改变且未注明。同一目录两套口径给出不同值（8.2.0：`949192…` vs `89876c5c…`），跨阶段直接比对会误判为漂移——我最初即触发此误判，重算口径后消解。建议在 `guard-*.tsv` 头部写死口径 | `tmp/phase9-evidence/guard-baseline.tsv` |
| P3-2 | `tmp/phase3h/regen-patch-id.sh:12` 仍 `printf 'component\tpatch\tstable_patch_id\n'` 三列。DECISIONS 称「无在用引用，备查不动」——我 grep 确认无脚本引用，声称属实；但它与四列口径共存，是下一次「三列/四列」同族事故的现成种子 | `tmp/phase3h/regen-patch-id.sh:12` |
| P3-3 | `run-linux.sh:16` 头注释依赖集含 `libzstd-dev`（DEV-P9-11 根因的文档源头）。执行者已建议登记、协调器已列为待办，**至本报告落稿仍未改** | `run-linux.sh:16` |
| P3-4 | 15.2.0 封存件 `generated_utc 2026-08-17T19:07:59Z` 与文件 mtime `2026-08-18 04:07:59`（+9）自洽；但 12.2.0 封存件 `15:32:16Z` / mtime `00:32:16`，两件相隔约 12.5 小时，简报 §7 称 15.2.0「待 linux 腿后签」——次序合理，惟两件签发时点差距未在封存头互相交代，跨件核对需自行换算时区 | 两份 `phase9-*-SHA256SUMS` |
| P3-5 | 4c 判据 v1 的失败留痕保留良好（`tflags-4c.judge.v1-FAIL.txt` + `tflags-4c.v1-FAIL/`），但 v1 失败的**真实根因**（并发交错导致「命令行集合」不是良定义的观测量，样本行可见 `vr-values.ccuu/usr/bin/clang++` 这类交错串）未在 DEV 台账写明，只记了「判据修订」。根因不记则同族会复发 | `tmp/phase9-evidence/15.2.0/s3/tflags-4c.judge.v1-FAIL.txt` |
| P3-6 | 简报 §4 称 rv64 半字扫描用「三 objdump 同 symlink 法」，但 12.2.0 `summary.json` 的 sides 只有 `OURS`/`WCH` 两侧（`side_count=2`），15.2.0 亦然。「三 objdump」应指工具数而非侧数，措辞易被读成三侧对照 | `briefs/S3-milestone.md:59` |
| P3-7 | `sr02-assert.txt` 首行账目算错：写「sides 6 -> 7；7^2*8=392」并在末行「rows 512（账目 288->392 由加侧解释）」——实测矩阵为 **8 侧 / 512 行**（8²×8）。简报 §3 的「512 行（加 OURS-P9 侧，8²×8）」才是对的，产物里的推导是 p8 时期的陈旧算式。实质无碍（我已确认 OURS-P9 在最终矩阵的 producer 与 decoder 两侧均在位，DEV-P9-08 自定判据成立） | `15.2.0/s3/sr02-assert.txt:1` |
| P3-8 | A/B/C/D 四类分歧表存在**重叠双计**：表头写「92 探针」而 A=16/B=26/C=20/D=20 求和 82；分工单元复算得真正有分歧者 66、零分歧 26，且 D 的 20 = A 的 16 + 撞车组 4（A ⊂ D，16 个探针被计两次）。协调器若按表求和会推出「82 有分歧、10 说不清」的错误图景 | 两份 `spec.md` §「分歧四类」、`briefs/S1-milestone.md:27-33` |
| P3-9 | 两处头条数字的**判据列集合从未列名**：「92 对 × 7 列 = 644」与「1472 单元 = 184 × 8 列」，而落盘 `probes.tsv` 有 9 个可比较列，且两处排除规则还不同（§3 排 `elf_class`+`attr_arch`，§4 只排 `attr_arch`）。独立复算者用全 9 列会得 828/1656，与文档全对不上 | 两份 `spec.md` §3/§4 |
| P3-10 | 「改动面上界已被实证限定」强于证据：所引 `c.ld`/`c.sd` 是 `INSN_CLASS_C` 行，**不经 `xw_enabled` 门也不经 XW 属性写出路径**；而我方侧 rv64 汇编失败使属性/mapping 两列全空，「XW 门与属性面在 rv64 正常」无正面观测（xlen 过滤在门测试之前 `continue`，遮蔽了该面）。缓解：分工单元读源码确认 `xw_enabled` 无 xlen 条件，且 S3 已按协调器回执④补 rv64 属性/mapping 抽探 8/8 | `briefs/S1-milestone.md:42-47`、两份 `spec.md` |
| P3-11 | 里程碑回执**无落盘件**：checklist「协调器规格确认回执」打勾，所引证据是 `briefs/S1-milestone.md`（**发件**而非回执），全树无回执原文。按 checklist 自定的「无证据不得打勾」标准属证据链断裂。缓解：回执内容由 `DECISIONS.md` 2026-08-18「里程碑 1/2 裁定追录（原件为跨会话信）」从协调器侧独立佐证 | `tmp/prompts/phase-9.checklist.md:41-45` |
| P3-12 | 其余文档级项（分工单元发现，我抽验属实）：12.2.0 spec §4「164 项全部成对出现」实为 160 成对 + 4 单侧（control 组 rv32 诊断后缀）；spec 中补丁行号在 S2 重导后全部漂移（15.2.0/0002 由 505… 移至 561…）；`s1-probe.py:63` 指针指向不存在的 `wch-xw.md`（实为 `ref/wch-isa-research/isa/custom/wch-custom-isa-reference.md:67-69`）；12.2.0 spec 以「同 15.2.0」代替本版立即数/寄存器约束整节（数值我复算无误，惟文档不自足）；`mapping-symbols.txt` 无生成脚本与 provenance | 多处 |
| P3-14 | S3 简报自相矛盾：`:78` 标题「偏差与纪律违反（**8 条**）」vs `:151`「偏差终态 **12 条**」；且 `:80`「全部停机均为**判据器缺陷**」窄化了实情（DEV-P9-09/10/11 属沿用件常量/容器前置/容器依赖等环境缺陷，`regression.md:79` 的「判据器或环境缺陷」才对） | `briefs/S3-milestone.md:78,80,151` |
| P3-15 | 「两版本 spec.md §N」类证据指针系统性只兑现一版本：DEV-P9-01 指「两版本 §2」、DEV-P9-03 指「两版本 §3」，实际只登记在 `15.2.0/spec.md:44-47` 与 `:87-88`，`12.2.0/spec.md` 通篇无偏差登记 | `deviations.tsv:3,5` |
| P3-16 | P9-F1 对 darwin 侧不受影响的**理由**与现文不符：台账称 `build-toolchain-15.2.0.sh`「只按列名取值不校验表头」，实际 `:203` 有 `head -1 … | grep -q 'source_commit'`——它校验表头且要求四列。结论（darwin 全程正常）仍成立，依据的是该行加入时刻（晚于 60ebcda）而非「不校验」。另 `tmp/phase3h/gate-prepare_linux.sh` 不在版本管理内，P9-F1 的 git 时序判定只覆盖两处命中中的一处 | `deviations.tsv:12` |
| P3-17 | `guard-baseline.tsv:2` 的哈希口径未写 `LC_ALL=C`，按字面在 macOS 默认 locale 下复算得不同值（`README.md` 排序位不同）；与 P3-1 的绝对/相对路径口径合并为同一类「口径未钉死」问题 | `guard-baseline.tsv:2` |
| P3-18 | 其余叙述/账目类（分工单元发现，抽验属实）：`v1-repro/NOTE.md:12` 把 8 个 **cell** 说成 8 个 **comparison**（zcb=1 profile×4 mode×2 side=8 cell → 4 comparison），因 v1 已毁不可对账；`12.2.0/round-report.md:52` 的 `compared=162 differing=0` 全仓只存在于报告、`p5/` 无判据器输出（逐对 `cmp` 复算属实）；`regression.md:37`「其中三条是判据器缺陷」按 tsv 自身 kind 列应为五条；`checklist:17-18` 并列「184 探针」与「七组 8+8+8+17+5+4+3=53」两个口径未说明 | 多处 |
| P3-19 | 本轮实质改动有一部分不在任何封存清单内：封存 scope 声明为 `patches/<v>/` 20 件（我已复核 20/20 OK），而同轮还改了 `scripts/build-toolchain-{12.2.0,15.2.0,15.2.0-linux}.sh`、`scripts/export-patches-{12,15}.2.0.sh`、`DECISIONS.md` 及未纳版本管理的 `tmp/phase3h/gate-prepare_linux.sh`（P9-F1 修点）。scope 是声明过的故非失实，但 4c 删除、P9-F1 修复、DEV-P9-02 补齐三项交付日后无 hash 可复核 | 两份 `phase9-*-SHA256SUMS` |
| P3-20 | 三片补丁的 commit message 指向 gitignored 的 `tmp/phase9-evidence/<版本>/spec.md`（硬约束 7 要求的规格来源指针，我已逐片确认文件存在）。但 `tmp/` 不随仓库走，上游 reviewer 或换机复核时该指针不可解析——与「每个补丁能向上游 reviewer 解释」的硬规则有张力，同族于 closure §7③ 的 full-lane 工具归属问题 | `patches/{15.2.0/0002,15.2.0/0007,12.2.0/0004}` |
| P3-13 | 12.2.0 官方 rv64 dejagnu 参考件只覆盖 12 形式（缺 4 个 sp 裸别名输入拼写），15.2.0 同名件为 16 形式。**我已实测关闭该风险**：`lbu/lhu/sb/sh a0,0(sp)` @`rv64imac_xw` 在两版本上官方与我方**目标文件逐字节相同**（`08802880 48806880`），且 `wch-xw-disassemble-rv64.d` 已覆盖全部 8 个 sp 编码的反汇编面 ⇒ 属**用例覆盖缺口**而非行为缺陷 | `12.2.0/s1/official-rv64-testcase-refs.txt` |

---

## 4. 正面确认

以下均为我**自己复算/实开命中**，构成放行建议的基础。

### 4.1 使命面（这是本阶段的存在理由，我给最高等级确认）

- **rv64 对齐达成且可判别**：两版本各 6/6 arch 配置下官方与我方在 rc/stderr/目标字节/三模式反汇编上**全同**；同一探针对清理前树仍报 `rc=1` ⇒ 通过不是空绿。
- **12.2.0 官方 rv64 面与 15.2.0 同形是实测事实**：我用 12.2.0 官方二进制直接验证，官方 `as -march=rv64imac_xw` 接受并编码 `2188/218a/a188/a18a`，`objdump -Mxw` 解出 `lbu/lhu/sb/sh`。任务书硬约束 1「禁止假设与 15.2.0 相同」在**结论层**没有被违反。
- **rv32 零扰动**：我方 phase-9 两棵树各跑一次 quick，**274/274/0**，`aux_diff=4` 全为既裁定 `.map` 类。这是对「rv32 面硬 gate」最直接的独立兑现。
- **rv64 半字穷举实质成立**：24 cell × 65536 半字、12 comparison 零失配、`-M xw` 增量 +8704 且两侧相同——我用两条独立路径各验一次。

### 4.2 交付面与纪律

- clean-room 复放 **6/6**（含两棵新 binutils 树），gcc 两树逐位未动，`patches/8.2.0` 内容哈希进出场**逐字相同**（硬约束 4「不触碰」成立）。
- 封存 **20/20 ×2**，均签在最后一次改动之后，封存后**零 mtime 漂移**；两件头部自述 seal-time 双树且与镜像现值一致——P2-9「先签后改」纪律本轮**未复发**。
- 守卫进出场三值（含 `toolchain-current`、`mrs`）与 `guard-s2exit.tsv` 逐字相同；我三次占用 comparator 后 symlink 均复位为基线。
- **保留历史**：两版本 `refs/openwch/phase9-pre-xlen-binutils` 均在位。
- **失败留痕纪律在 DEV-P9-07 之后确实执行**：`tflags-4c.judge.v1-FAIL.txt`、`tflags-4c.v1-FAIL/`、`halfword-rv64.v1-repro/` 三处 v1 留痕在盘，未被覆盖。
- **判据器设计本身是本项目迄今最好的一版**：`halfword-judge.py` 的 J1（每 cell 必须解满 65536 且 missing=0）直接封死 DEV-P9-04 的「空对空」；J4 防静默丢 cell；**J5 强判据**要求 `-M xw` 相对 `default` 的 XW 命中**严格增加且两侧增量相等**——这是「自证判别力」被真正机器化的形态，比 p8 的口头纪律前进一大步。
- **ra-flags 常设护栏未被本轮破坏**：我自跑 p8 的 reproducer，`OFF == P9` 逐字节相同、`OFF != P8` ⇒ 护栏仍能判别 P8-R 修掉的那个缺陷。
- **closure §7② 落地**：三版本 `patch-id.tsv` 均四列含 `source_commit`，12.2.0 `From` 由全零改真实 sha ⇒ 我在 p8 报告里记的 P3-17 至此闭合。

### 4.3 P9-F1（协调器责任条）

- **时序自行重推成立**：`d681881`（08-17 21:31:45）是 `60ebcda`（08-17 23:12:16）的祖先；我用 `git show 60ebcda:scripts/build-toolchain-15.2.0-linux.sh` 确认该 commit 时点该处仍是 `NF != 3 || $0 != "component\tpatch\tstable_patch_id"` 的**三列断言** ⇒ 「四列统一未同步两处表头校验、自 60ebcda 起 linux gate 无法起跑」**属实**。
- **「p8 结论无损」逻辑链成立**：p8 linux 腿的证据目录 mtime 为 08-17 16:51–17:51，早于 21:31 与 23:12 两个时点 ⇒ 它跑在缺陷引入之前，结论不受影响。
- **修法正反自证**：两处现文均已改为四列并校验第四列名；本轮 linux 全程重跑（quick 274/274、full 47797/47797、XW+LTO SEALED）**重新兑现**了 linux 面，不是靠不变性勾定。
- **责任条如实**：DECISIONS 明写「系统一批次的协调器遗漏」，并连带登记了 `regen-patch-id.sh` 三列与 `run-linux.sh:16` 注释两项。**协调器对自身缺陷的登记是充分且不回避的**，这一条我给正面评价。

---

## 5. 协调器行为审计

| 审计点 | 结论 |
| --- | --- |
| M1 规格确认 | 裁定「两版本 xlen 统一 0」——**结论经我实测正确**。裁定同时强制了「A 类须在 S3 有显式裸别名判别行」「rv64 属性/mapping 抽探加进 S3」「两版本 dejagnu 期望文本各取各官方禁统一」，三项我都在盘面见到落地（后者由用例命名分版本佐证）。**加判据而非减判据，方向正确** |
| M2 S2 验收 | 「改后判据器对改前树预封存」记为正面纪律动作——这正是自证判别力的机器化，与本轮 J5、ra-flags 一脉相承。DEV-P9-02 授权就地修（导出脚本补 `--dry-run` 与未知参数拒绝）合理 |
| **4c 判据修订批准** | **合理但不充分**。v1 判据（构建命令行集合）确实测的是被 `-j16` 交错破坏的非良定义观测量，换掉有正当理由（与 p8 SR-02 `rc` 列同型）。但修订后的三条腿对被归因机制**全部无判别力**（P2-2），协调器批准时未要求「新判据须能判别旧问题」——而这恰是本阶段自己在 J5 上立的标准。**同一会话内两套尺度** |
| P9-F1 责任条 | **如实、充分**，含影响面、时间边界、连带项，无淡化 |
| 里程碑验收结构 | 见 P2-4：简报是唯一入口，无「简报 vs 产物」机械对照环节 |
| 与 p8 教训的衔接 | DEV-P9-08 自记为「p8 同族第 5/6 次复发」并提炼出**正确的新判据**（「被测侧是否真出现在最终矩阵里」而非「我改了 SIDES」）——这是把 p8 RV-1 的教训真正吃进去的表现 |

---

## 6. 我没能核验的项

1. **linux 腿全部数字**（quick 274、full 47797、XW+LTO SEALED、prepare 复放）：容器内运行，本次只读审计未起容器，仅对 `linux-results.tsv` 等产物做了转录一致性检查。
2. **DEV-P9-11 的三方 libzstd 复现**：`a006bd2388202b94`（官方/p8 我方）与 `521c1e26835eaea8`（本轮首跑）两值我未重算（需容器内构建）。
3. **15.2.0 SR-01/03、DCXW、XW+LTO、testsuite 的重跑**：本轮因 binutils 变更而全套重跑（非不变性勾定，方向正确），我只做了产物转录核对，未重跑。（**SR-02 已不在此列**：我已定位最终矩阵并确认 512 行 = 8 侧²×8，`OURS-P9` 在 producer 与 decoder 两侧均在位 ⇒ DEV-P9-08 自定判据成立，见 §4.2。）
4. **DEV 台账 12 条的逐条对原始件核验、regression.md 的需求/设计回归**：分派的第二个分工单元未在本报告落稿前返回。我自行独立覆盖了其中 DEV-P9-04（判据器空对空 → J1 修法，已重算）、-07（留痕纪律，17 处 `*FAIL*`/`v1-*` 留痕在盘）、-08（侧表族，见上）、-02（导出脚本 `--dry-run`，间接见 4c 面），**未覆盖** DEV-P9-01/03/05/06/09/10 与 DEV-P9-11 的三方哈希复算，及 regression.md 全文。
5. **A 类 16/16 显式裸别名判别行**：我见到 `12.2.0/s3/p5/out/*q01-explicit-*` 系列产物存在，未逐条打开比对 16 行。但 A 类现象本身我已独立实测确认（`lbu a0,0(a1)` @rv64 两版本官方均 rc=0 发 2 字节 `8821`；sp 别名发 `08802880 48806880`，官方与我方逐字节同）。
6. **linux 腿全部数字**与 **DEV-P9-11 的 libzstd 三方哈希**：需容器，未起。
7. **12.2.0 我方侧的 S1 改前基线（342 及 A/B/C/D 我方半边）**：被测二进制已被 S2 重建覆盖（P2-6），无冻结副本，物理上不可复算。
8. `ref/wch-toolchain-private.md` 按约束禁读；`patches/8.2.0` 与 `tmp/toolchain_8.2.0` 按硬约束 4 未触碰，故 8.2.0 的 xlen=0 rv64 参照系那条证据链未核验。

---

## 7. 验收建议

**有条件放行。**

使命面（rv64 与各自官方对齐、rv32 零扰动、三版本现均以 `xlen=0` 编码 XW 行）经我多路独立复算**全部成立**，且本轮的判据器设计（J1/J5、must-fire、预注册、留痕）是本项目迄今最成熟的一版。**没有 P0**：无补丁语义缺陷、无 gate 数字失真、无行为回归。

**两项 P1 都不是"结论错"，而是"纪律在本阶段自己立的规矩上破了口"**，且都落在本项目反复付过学费的同一族：
- **P1-1**：预注册文件第 4 行专门为防「测量后换判据」而写、并点名 p8 SR-02 教训；4c 恰好在测量 FAIL 后换了判据、换成两条恒真判据、且没走它自己规定的日期修订段。这是本轮唯一一处**规程性**违反。
- **P1-2**：DEV-P9-07 立规之后再犯，两次 linux 停机原文永久丢失，轮报告把 8 次停机报成 3 次。

**入库前必须闭合（P1）**
1. **P1-1**：把 4c 的判据替换补登为独立偏差 + 补写预注册日期修订段 + 向协调器补批；结论改挂到「构建成功 + rv32 gate 传递性等值」；如实登记 `zaamo_zalrsc` 12→5。
2. **P1-2**：三条台账指针改指存活件、注明 run1/run2 原文不可复算、停机数改为实数并附对照表。

**入库前建议闭合（P2，证据件层，工作量小）**

1. **P2-1**：用现行判据器对 12.2.0 重跑 `halfword-judge.py`（数据不必重跑），覆盖过期的 `halfword-rv64.judge.txt`；简报同步。
2. **P2-2**：把 4c 结论明确挂到「构建成功 + rv32 gate 产物传递性等值」两条上，并如实登记 `zaamo_zalrsc` 12→5 这一构建期可见差异；不再以 T2a/T2b 作为独立证据陈述。
3. **P2-4**：里程碑验收增加一次「简报数字 vs 产物」机械对照。
4. **§6-3**：补一次 SR-02 最终矩阵含 `OURS-P9` 侧的机械确认（用 DEV-P9-08 自己提炼的判据）。

5. **P2-5 / P2-6**：把真正产出 S1 证据的脚本版本落盘并在 spec §8 指明；在 spec §1 与 round-report 标注「12.2.0 我方侧已随 S2 重建失效、改前基线 342 不可复算」。今后 S1 侧参一律先冻结再探测。
6. **P3-8 / P3-9**：A/B/C/D 表补 Z 行 26 并标注 A ⊂ D 的 16 项重叠；两处头条数字补列判据列名。这两项直接影响协调器读表得出的图景。

**建议登记**：P2-3（宿主构建非比特可复现及其证据后果）、P3-1（守卫哈希口径）、P3-3（`run-linux.sh:16`，已列待办）、P3-13（12.2.0 sp 裸别名 rv64 用例缺口——行为无碍，补用例即可）。

**补充说明（供协调器权衡）**：本轮我给出的 P2 全部落在**证据件与叙述**层，没有一条指向补丁语义、gate 数字或行为面；而使命面的四项核心结论（rv64 两版本对齐、rv32 零扰动、rv64 半字判别力、护栏保持）我都用**不依赖本阶段任何产物**的路径重新导出过一次。就「结论是否可信」而言，本阶段是我审过的三轮里证据最经得起独立重算的一轮；扣分集中在「盘面能否自证」，而非「事实是否为真」。

---

## 附：审计脚手架

- clean-room 复放脚本（与 p8/P8-R 同一份，未修改）：`/Users/apple/Projects/openwch/tmp/phase8-evidence/review-s4/replay.sh`
- rv64 探针与半字重数脚本：`/tmp/p9audit/`
- `-fcall-saved-ra` 护栏 reproducer（p8 审计遗留，本轮复用）：`/tmp/p8audit/ra/fi.c`
- 本次审计未修改任何 phase-9 交付文件；两次 comparator 占用 `tmp/golden/toolchain-current`，前/中/后三次读数均为 `…/tmp/phase3g-evidence/ours-v3.0-frozen`，已复位。

---
---

# 修复轮 delta 复核

复核对象：`tmp/phase9-evidence/briefs/S3-fix-report.md`（六件修复）。同约束：可复算即自己复算。

**delta verdict：放行。** 两项 P1 **全部闭合**，四项 P2 **三项闭合、一项部分闭合**。修复质量显著高于我提出的要求：其中两处执行者**推翻了我/协调器的设想并给出更强做法**，一处**自主扫尾找出我漏点的第四实例**，一处**把影响面自主上修**。新增残留 **1 P2 + 2 P3**。

## D.1 逐项闭合判定

| 项 | 判定 | 我的独立复算 |
| --- | --- | --- |
| **P1-1** 4c 证据链重构 | **闭合（强）** | 见 D.2 |
| **P1-2** 留痕与停机计数 | **闭合** | 见 D.3 |
| **P2①** p8 证据树写入 | **部分闭合** | 账目与封存面闭合；两项具体整改未做，见 R-1 |
| **P2②** 侧表族 | **闭合，且超出要求** | 见 D.4 |
| **P2③** `/Users/wch` 正证 | **闭合** | 见 D.5 |
| **P2④** J5 重生成 | **闭合** | 见 D.5 |
| 修复轮自身 FAIL 留痕与归一范围 | **合规** | 见 D.6 |

## D.2 P1-1：我把新证据链整条重跑了一遍

**① 判别力自证是真实测所得，且 fail-closed。** 我用自己的 out-dir 独立运行 `probes/tflags-discriminating.sh`，输出与执行者逐条相同：

```
same specs / sysroot-sfx / multi-lib
DIFFERS search-dirs · multi-dir · predef-c · predef-cxx      <- 4/7
A PASS：4/7 类输出不同
（multi-dir: with=rv32imac_zaamo_zalrsc/ilp32 / without=.）
```

设计上这条比我原先设想的更干净：**同一棵已构建树的同一个 `xgcc`**，只切换 TFLAGS——完全绕开了宿主码生成不可复现这一干扰源，是对该命题的直接实验。`multi-dir` 的 `rv32imac_zaamo_zalrsc/ilp32` vs `.` 是「探针确实选中了不同 multilib」的**直证**。脚本 `:72` 在 7 类全同时 `exit 1`，fail-closed 成立。

**② 命题精确化忠于被删注释原文（我从 git 取原文核对，非采信转述）。** `git show HEAD:scripts/build-toolchain-15.2.0.sh:68-70`：

> `...does not enter host CFLAGS or change configure argv, the installed multilib table, or the installed driver's behavior;`

注释声称四件事，**其中不含「构建期探针 argv 不变」**。新命题「TFLAGS 改变构建期探针 argv 与其即时输出，但不改变安装产物」**忠于原文**，且 B 部分逐条测了其中两件可测的（installed multilib table、installed driver's behavior，后者拆成 specs / 默认预定义宏 / 搜索路径三面）。

**③ 安装树对照复算命中**：路径集差异 **0**、逐字节相同 **2875**、差异 **47**、非可执行差异件 **0** —— 与报告逐值相同。

**④ 预注册 J 节在位且不掩饰时点**：`s2-preregistration.md` J 节首句即「**本段写在测量之后**，是对一次违反本文件 `:4` 条款的行为的补记」，正文自陈「**这是违纪，不是判断分歧**」。这是我能想到的最诚实的形态。

**⑤ 我的「T2a/T2b 恒真」判定被完整采纳**：J 节与 `round-report.md §5.1` 逐条复述并**独立复核确认**，且执行者**主动撤回**了自己「TFLAGS 从未被引用」的技术结论。`tflags-discriminating.sh` 的头注释把这段错误分析完整保留在代码里——错误叙事不被抹除。

**⑥ 协调器设想的 gcc 二进制比对路径确被证伪，我独立确认**：`cc1/cc1plus/lto1/collect2/gcc/g++/cpp/lto-dump` **8/8 DIFFERS**。这同时是对我 RV-2（宿主构建非比特可复现）的**第三次交叉验证**——执行者拒绝该路径并改用不依赖该前提的方法，判断正确。

## D.3 P1-2：闭合

- `deviations.tsv` 新增 **DEV-P9-12/13/14/15**，台账终态 **16 条**（15 DEV + 1 P9-F1，我按 id 去重实点）。
- `round-report.md:123` 起有 **8 行逐次停机对照表**，含 **`原文存活`** 列，第 3/4 行明标 **否** 并交叉引用 DEV-P9-12。我此前存疑无归属的 **run3** 现为第 5 行，归入 DEV-P9-09 连带。
- 指针改指存活件已办：DEV-P9-09 证据列现为 `linux-results.run1-FAIL-prepare.tsv` 并原地标注「run1 报错原文不可复算：`prepare.stderr` 已被覆盖为 0 字节，见 DEV-P9-12」。两个 0 字节文件现在**只以「本条即其说明」的身份**出现在 DEV-P9-12 中，不再充当证据。`linux-build.run5-FAIL.stderr` 87 字节原文存活。
- DEV-P9-12 自陈「DEV-P9-09/P9-F1/DEV-P9-10 三条台账当时正指向这两个 0 字节文件当证据」——自证其罪，未淡化。

## D.4 P2②：闭合且超出要求

- 第三处（PASS 文案）与**第四处（我漏点的 `:61-63` 由历史侧数 `len(hist)+1` 递推）**均已修；现文 `assert-sr02.py:63-67` 改为**从实测新矩阵导出侧集**并打印侧名。
- 重生成产物：`A rows: 3h=288 new=512 (sides 6 -> 8; 6^2*8=288, 8^2*8=512) sides=OURS,OURS-3H,OURS-P8,OURS-P9,OURS-V2,UPSTREAM-MATCHED,UPSTREAM-P2,WCH` —— **8 侧全列名、算式自洽**。我原报告的 P2-8/P3-7 至此闭合。
- **无第五处**我独立扫尾验证：phase-9 在用件中含 `OURS-P8` 的 7 个 `.py/.sh` **全部同时含 `OURS-P9`**（逐件计数在册）。`assert-sr03.py:12,28` 已改为侧名由 `argv[2]` 传入、不硬编码。
- 旧产物 `sr02-assert.v1-p8arith.txt` 留痕在盘。

## D.5 P2③④：闭合

- 新增 `guard-s3exit.tsv`，含 `dirlist wch-Work` / `dirlist wch-root` / `readlink` 两条 + 一条正面证据 `mtime wch-newest = 0`。**我自己复算 `find /Users/wch -newermt '2026-08-17 14:00'` = 0**，目录列表与记录相同 ⇒ 我原报告 P2-9 闭合。
- 12.2.0 `halfword-rv64.judge.txt` 重生成为 **5/5 PASS**，`xw 增量 {'OURS': 8704, 'WCH': 8704}` —— **与我此前两条独立重算（自数 `decoded.tsv.gz`、用现行判据器重跑）逐值相同**，且与 15.2.0 同值 ⇒ P2-1 闭合。旧件留痕 `halfword-rv64.judge.v1-stale.txt`。

## D.6 修复轮自身的判据器假 FAIL：披露合规，归一严格限于路径

- 留痕齐全：`tflags-4c.judge.v1-FAIL.txt` / `tflags-4c.v1-FAIL/` / `tflags-4c-discriminating.v1-FAIL{,.judge.txt}` / `tflags-4c-v2-FAIL-searchdir{,.judge.txt}` —— **4c 一条线上保留了四份失败产物**。修复轮对自己也执行了 DEV-P9-07 纪律。
- **归一范围**：`sed` 在整个 `tflags-discriminating.sh` 中**只出现 2 次**（`:97-98`），且只作用于 `-print-search-dirs` 的输出、只把各自安装根替换为 `<ROOT>`。B2 的安装树全量对照走的是裸 `cmp`，**未归一**。
- **gate 面零 normalize 未破**：`scripts/evt-compare.sh` 的 sha256 仍为 `7ee93e19b5ea50b2…`，与 p8/p9 基线逐字相同 ⇒ 比较 harness 未被本轮触碰。

## D.7 delta 新发现

### R-1 `[P2][置信度 高]` P2① 的「登记」半边做足了，「整改」半边未做——同一越界会在下次沿用时复发

影响面量化与封存面证明我都复算通过（见 D.8），但我原报告 P2-7 点名的**两项具体整改**都没做：

| 项 | 现状 |
| --- | --- |
| DEV-P9-09 disposition 仍含被证伪的句子 | 现文仍为「…不触碰 p8 的 12G 证据树。…**修后复查全文件无 phase8 残留常量**。」——而 DEV-P9-14 同表内已承认向 p8 树写入 1158 文件。**同一份台账内两条自相矛盾** |
| 残留常量未改 | `15.2.0/s3/replay.sh:15` 仍 `SCRATCH="$REPO/tmp/phase8-evidence/15.2.0/scratch"`（且被 `run-darwin.sh:252`、`resume-from-sr02.sh:144` 实际调用）；`15.2.0/s3/probe-ra-flags.sh:23` 默认输出根仍 `tmp/phase8-evidence/…` |

**失败场景**：下一轮沿用这套 runner，1158 文件级越界写入**原样复发**，而台账里那句「修后复查全文件无 phase8 残留常量」会让复查者以为已清。
**修法**：删除或改写该句（改为可核窄口径「`run-linux.sh` 内 NS 常量已改」）；把两处常量改到本轮证据根，或在 DEV-P9-14 明列「常量未改，复发风险已知」。

### R-2 `[P3][置信度 高]` 1158 的口径是**子树限定**而非时间截断，台账未写明——我自己因此先误判了一次

我初次用时间截断复算（`find tmp/phase8-evidence -newermt '2026-08-17 23:00'`）得 **143,514**，远大于 1158。追查后确认：多出的绝大部分是 `12.2.0/scratch/replay/**`（142,303 件，mtime **23:09:23**），属 closure §7② 四列重导轮（`60ebcda` 23:12）的产物，**早于 phase-9 S0（23:39），与 phase-9 无关**；且 phase-9 无任何脚本引用 `phase8-evidence/12.2.0`。按执行者的四个子树口径复算得 **1160**，与 1158 相差 2（目录项/窗口边界），**账目可复算、结论正确**。
**修法**：DEV-P9-14 补一句口径说明（「按 SR-02/replay 四个输出子树计，非时间截断」），否则下一个复核者会重复我这次误判。

### R-3 `[P3][置信度 中]` 被删注释四项声称中，两项由论证而非测量覆盖

B 部分实测了 `the installed multilib table` 与 `the installed driver's behavior`（三面）。另两项 `does not enter host CFLAGS` 与 `change configure argv` 仍由源码论证（原 T2c）覆盖——对本命题而言风险极低（TFLAGS 挂在 `make` 而非 `configure`），但严格说 B 覆盖 2/4。建议在 judge 输出里标注这一覆盖边界。

## D.8 delta 正面确认（我自己复算的）

- `tflags-discriminating.sh` 我独立重跑，A 4/7、B 四项 IDENTICAL、B2 2875/47/0 **逐值命中**。
- 被删注释原文我从 `git show HEAD:` 取，命题精确化**忠于原文**。
- gcc 侧 **8/8 DIFFERS** 独立确认（RV-2 第三次交叉验证）。
- **p8 交付面封存独立复验**：`patches/8.2.0` 对 `phase8-8.2.0-SHA256SUMS` **10/10 OK**（与协调器现场结果一致）；三份 p8 封存件自身 sha 与 mtime 均在 phase-9 S0 之前（8.2.0 19:03、15.2.0 23:01、12.2.0 23:10）；两份 p8 封存清单 `grep -cE "sr02|scratch"` = **0** ⇒ **1158 文件越界写入与 p8 交付面零交集，p8 结论不受影响**，这一层论证成立。
- 1158 账目按子树口径复算 = 1160，可复算（见 R-2）。
- 无第五处侧表：7/7 在用件 `OURS-P8`/`OURS-P9` 共现。
- `/Users/wch` 零改动我独立复算 = 0 条目。
- 12.2.0 J5 = 5/5、增量 8704 双侧，与我先前两次独立重算一致。
- 台账 16 条按 id 去重实点；8 行停机表含原文存活列。
- 修复轮四份 FAIL 留痕在盘；`sed` 仅 2 处且限于搜索路径；`evt-compare.sh` sha 未变。

## D.9 最终 verdict

**放行。**

phase-9 的使命面结论在首轮已由我多路独立复算全部成立；本修复轮把首轮扣分的**纪律与证据面**逐条补齐，且补齐方式经得起独立重跑——新 4c 证据链是本项目迄今**唯一一条自带 fail-closed 判别力自证、且命题与被删代码自述逐字对表**的归因证据。

三处值得记入项目经验：
1. **执行者三次推翻上游设想并给出更强做法**（否决 gcc 二进制比对、自主上修影响面、扫尾找出我漏点的第四实例），且每次都附证伪实据——这是「不迎合审计意见」的正确形态。
2. **预注册 J 节与 round-report §5.1** 是本项目最完整的一次自我撤回：违纪定性（「这是违纪，不是判断分歧」）、错误叙事保留在代码注释里、时点不掩饰。
3. **R-1 提示一个结构性倾向**：本轮（以及 p8 若干条）多次出现「登记做足、整改未做」。登记让问题可见，但**残留常量不改则同一越界必然复发**。建议协调器把「整改与登记同批完成」写成收尾条件，而非二选一。

**入库前建议**：闭合 R-1（一句话改写 + 两处常量，或明列复发风险）；R-2/R-3 随文档同批。
