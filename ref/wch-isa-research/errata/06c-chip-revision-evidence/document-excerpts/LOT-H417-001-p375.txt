CH32H417 系列应用手册                                     https://wch.cn


                           IBI 标志：
                           当 I3C 用作主设备时，该标志由硬件置 1，用于指
                           示已接收到 IBI 请求。
                           该标志的清零方式是通过软件向 R32_I3C_CEVR 寄
       15   IBIF        RO 存器 CIBIF 位写入 1。                           0
                           注：（1）此位仅适用于主设备；
                           （2）批号倒数第五位小于 3 的芯片，在接收从机
                           发送的不带数据的 IBI 请求时，该位不生效。此
                           类包的处理具体请参考官网 EVT 例程。
  [14:12] Reserved      RO 保留。                                       0
                           标志：
                           该标志由硬件置 1，用于指示发生错误。然后，软
       11   ERRF        RO 件必须读取 R32_I3C_STATER 以获取错误类型。             0
                           该标志的清零方式是通过软件向 R32_I3C_CEVR 寄
                           存器 CERRF 位写入 1。
                           从设备发起的读结束标志：
                           当 I3C 用作主设备时，并且只有在禁止 S-FIFO
                           （R32_I3C_CFGR 寄存器 SMODE 位为 0）时，
                           该标志才会由硬件置 1，用于指示从设备已提前结
                           束读传输。然后，软件必须读取状态寄存器
                           R32_I3C_STATR 以检查与最后一条消息相关的信
       10   RXTGTENDF   RO                                           0
                           息，并获取提前结束的读传输中接收到的数据字
                           节数
                           （R32_I3C_STATR 寄存器 XDCNT）。
                           该标志的清零方式是通过软件向 R32_I3C_CEVR 寄
                           存器 CRXTGTENDF 位写入 1。
                           注：此位仅适用于主设备。
                           帧完成标志：
                           当 I3C 用作主设备时，该标志由硬件置 1，用于指
                           示 I3C 总线上的帧已（正常）完成，例如在发出停
                           止位时。
                           当 I3C 用作从设备时，该标志由硬件置 1，用于指
       9    FCF         RO                                           0
                           示在 I3C 总线上发送至该从设备或由该从设备寻
                           址的消息已（正常）完成，例如在主设备发出下一
                           个停止位或重复起始位时。
                           该标志的清零方式是通过软件向 R32_I3C_CEVR
                           寄存器 CFCF 位写入 1。
       8    Reserved    RO 保留。                                       0
                           最后读取的数据字节/字标志：
                           当 I3C 用作主设备时，该标志由硬件置 1，用于指
                           示必须从 RX-FIFO 读取的消息的最后一个数据字
       7    RXLASTF     RO 节/字（具体取决于 I3C_CFGR 寄存器 RXTHRES 位）
                                                           。         0
                           当读取消息的最后一个数据字节/字时，该标志由
                           硬件清零。
                           注：此位仅适用于主设备。
       6    TXLASTF     RO 最后写入的数据字节/字标志：                            0

V1.7                          371
